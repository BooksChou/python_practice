"""
给定一个无序数组，包含正数、负数和0，要求从中找出3个数的乘积，使得乘积最大，要求时间复杂度：O(n)，空间复杂度：O(1)
输入：  4  2  3  1 
输出：  24

思路 将列表排序（升序排列），取前两个数与最后一个数的乘积（--+）以及最后三个数的乘积（+++）的最大值
"""


l = int(input())
n = list(map(int,input().strip().split()))
n.sort()
 
tmp = max(n[0]*n[1]*n[-1], n[-1]*n[-2]*n[-3])
 
print(tmp)
